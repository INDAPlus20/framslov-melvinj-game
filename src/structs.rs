//Structs in separate file
//Copied into script-folder for easy peasy scripting
pub struct InputState {
    pub placeholder_a: isize
}

pub struct GameState {
    /*pub neutral_balls: Vec<Ball>,
    pub enemy_balls: Vec<Ball>,
    pub friendly_balls: Vec<Ball>,
    pub enemy: Character,
    pub character: Character,*/
    pub placeholder_a: isize
}

pub struct Character {
    pub placeholder_a: isize
}

pub struct Ball {
    pub placeholder_a: isize
}